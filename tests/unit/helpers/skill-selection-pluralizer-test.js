import { skillSelectionPluralizer } from 'portfolio/helpers/skill-selection-pluralizer';
import { module, test } from 'qunit';

module('Unit | Helper | skill selection pluralizer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = skillSelectionPluralizer([42]);
  assert.ok(result);
});
