import { skillMiniImage } from 'portfolio/helpers/skill-mini-image';
import { module, test } from 'qunit';

module('Unit | Helper | skill mini image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = skillMiniImage([42]);
  assert.ok(result);
});
