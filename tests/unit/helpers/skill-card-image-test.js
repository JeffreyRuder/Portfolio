import { skillCardImage } from 'portfolio/helpers/skill-card-image';
import { module, test } from 'qunit';

module('Unit | Helper | skill card image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = skillCardImage([42]);
  assert.ok(result);
});
