import { cleanCSS } from 'portfolio/helpers/clean-css';
import { module, test } from 'qunit';

module('Unit | Helper | clean css');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cleanCSS([42]);
  assert.ok(result);
});
